# goreleaser config for docket. Produces static, single-file binaries for
# linux/darwin on amd64/arm64 and packages them as tar.gz assets matching the
# names the install script expects (docket_<os>_<arch>.tar.gz).
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: docket
    main: .
    binary: docket
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w -X github.com/tvdavies/docket/internal/cli.Version={{.Version}}

archives:
  - id: docket
    formats: [tar.gz]
    name_template: "docket_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"

release:
  draft: false
  prerelease: auto

changelog:
  use: github
  sort: asc
