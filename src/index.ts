/**
 * tadu — TAsk DUrable.
 *
 * Library core: a file-backed task store. The CLI in `cli.ts` is a thin wrapper
 * over these functions so a harness can also import them directly.
 *
 * Nothing is implemented yet — see DESIGN.md for the intended surface
 * (createTask, appendComment, attachFile, link, move, contextBundle, query).
 */

export const VERSION = "0.0.0";
